# Supermarket-Billing-and-Inventory-System
This repository contains a C program that works as an inventory and billing system. It lets you manage clients, products, and simulate purchases inside a small supermarket environment.
It generates a .txt file for every invoice you create. You need a C compiler and you must save the project in a specific folder where the invoices will be stored.It generates a .txt file for every invoice you create. You need a C compiler and you must save the project in a specific folder where the invoices will be stored.
